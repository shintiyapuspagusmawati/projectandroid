<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_kontak_dialog"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="linear.com.projectpertama.KotakDialog">

    <Button
        android:layout_width="fill_parent"
        android:id="@+id/toastBtn"
        android:layout_height="wrap_content"
        android:text="@string/toasTextBtn">
    </Button>
    <Button
        android:layout_width="fill_parent"
        android:id="@+id/exitBtn"
        android:layout_height="wrap_content"
        android:text="@string/exitTextBtn"
        android:layout_below="@+id/toastBtn">
    </Button>
    <Button
        android:layout_width="fill_parent"
        android:id="@+id/listDialogBtn"
        android:layout_height="wrap_content"
        android:text="@string/listDialogTextBtn"
        android:layout_below="@+id/exitBtn">
    </Button>

</RelativeLayout>
